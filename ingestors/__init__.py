"""
PreMemora ingestors — data source connectors for the prediction engine.

Each ingestor normalizes external data into event dicts ready for
downstream processing (e.g. Graphiti ingestion, edge calculation).
"""
